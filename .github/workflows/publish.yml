name: Release

on:
  push:
    tags:
      - "v[0-9]*.[0-9]*.[0-9]*"
  workflow_dispatch:
    inputs:
      tag:
        description: Annotated release tag to recover
        required: true
        type: string
      run_id:
        description: Failed release run containing verified artifacts
        required: true
        type: string

env:
  RELEASE_TAG: ${{ github.event_name == 'push' && github.ref_name || inputs.tag }}

permissions:
  contents: read

concurrency:
  group: publish
  cancel-in-progress: false

jobs:
  build:
    name: Build and verify
    permissions:
      actions: read
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true
          version: "0.12.9"

      - name: Fetch current main
        run: git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main

      - name: Verify release source
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
        run: ./scripts/check-release.sh

      - name: Verify recovery source
        if: github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ github.token }}
          RECOVERY_RUN_ID: ${{ inputs.run_id }}
        run: ./scripts/check-release-recovery.sh "$RELEASE_TAG" "$RECOVERY_RUN_ID"

      - name: Download recovered release artifacts
        if: github.event_name == 'workflow_dispatch'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          github-token: ${{ github.token }}
          name: release-artifacts
          path: dist
          run-id: ${{ inputs.run_id }}

      - name: Set up Vite+
        if: github.event_name == 'push'
        uses: voidzero-dev/setup-vp@1b32467adbe183473499fd9d5d372c3ed9641754 # v1.18.0
        with:
          version: 0.2.4
          sfw: true
          cache: true
          run-install: |
            - args: ['--frozen-lockfile']

      - name: Enable pnpm
        if: github.event_name == 'push'
        run: corepack enable pnpm

      - name: Install AnyWidget test browser
        if: github.event_name == 'push'
        run: pnpm --filter @marimo-export/internal-loader-anywidget exec playwright install --with-deps --only-shell chromium

      - name: Install Python workspace
        if: github.event_name == 'push'
        run: uv sync --all-packages --all-extras --locked

      - name: Run release gate
        if: github.event_name == 'push'
        run: make check

      - name: Build release artifacts
        if: github.event_name == 'push'
        run: make package

      - name: Rebuild recovered Python source archive
        if: github.event_name == 'workflow_dispatch'
        run: |
          mkdir -p dist/python/from-sdist
          uv build --wheel dist/python/*.tar.gz \
            --out-dir dist/python/from-sdist

      - name: Verify recovered release artifacts
        if: github.event_name == 'workflow_dispatch'
        run: python scripts/verify_release_artifacts.py

      - name: Upload release artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-artifacts
          path: |
            dist/SHA256SUMS
            dist/npm/*.tgz
            dist/python/*.whl
            dist/python/*.tar.gz
          retention-days: 1
          if-no-files-found: error

  attest:
    name: Attest build provenance
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      actions: read
      artifact-metadata: write
      attestations: write
      contents: read
      id-token: write
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-artifacts
          path: dist

      - name: Attest release artifacts
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: |
            dist/SHA256SUMS
            dist/npm/*.tgz
            dist/python/*.whl
            dist/python/*.tar.gz

  publish-npm:
    name: Publish npm packages
    needs: attest
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: npm
      url: https://www.npmjs.com/package/@marimo-team/marimo-export
    permissions:
      actions: read
      contents: read
      id-token: write
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          package-manager-cache: false

      - name: Download release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-artifacts
          path: dist

      - name: Publish npm packages
        run: ./scripts/publish-npm.sh "dist/npm/marimo-team-marimo-export-${RELEASE_TAG#v}.tgz"

  verify-npm:
    name: Verify npm packages
    needs: publish-npm
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          package-manager-cache: false

      - name: Enable pnpm
        run: corepack enable pnpm

      - name: Download release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-artifacts
          path: dist

      - name: Verify public npm packages
        run: RELEASE_VERSION="${RELEASE_TAG#v}" ./scripts/verify-npm.sh

  publish-pypi:
    name: Publish Python package
    needs: verify-npm
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: pypi
      url: https://pypi.org/project/marimo-export/
    permissions:
      actions: read
      contents: read
      id-token: write
    steps:
      - name: Set up uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: false
          version: "0.12.9"

      - name: Download release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-artifacts
          path: dist

      - name: Publish Python package
        run: >-
          uv publish
          --check-url https://pypi.org/simple
          --trusted-publishing always
          dist/python/marimo_export-*.whl
          dist/python/marimo_export-*.tar.gz

  verify-pypi:
    name: Verify Python package
    needs: publish-pypi
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Set up uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: false
          python-version: "3.12"
          version: "0.12.9"

      - name: Download release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-artifacts
          path: dist

      - name: Verify public Python package
        run: RELEASE_VERSION="${RELEASE_TAG#v}" ./scripts/verify-pypi.sh

  release-notes:
    name: Create GitHub release
    needs: [verify-npm, verify-pypi]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      actions: read
      contents: write
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-artifacts
          path: dist

      - name: Create GitHub release
        run: |
          artifacts=(
            dist/SHA256SUMS
            dist/npm/*.tgz
            dist/python/*.whl
            dist/python/*.tar.gz
          )
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release edit "$RELEASE_TAG" \
              --title "marimo-export $RELEASE_TAG" \
              --verify-tag
            gh release upload "$RELEASE_TAG" "${artifacts[@]}" --clobber
          else
            gh release create "$RELEASE_TAG" "${artifacts[@]}" \
              --generate-notes \
              --title "marimo-export $RELEASE_TAG" \
              --verify-tag
          fi
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
