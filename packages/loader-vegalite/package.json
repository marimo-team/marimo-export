{
  "name": "@marimo-team/export-loader-vegalite",
  "version": "0.1.0",
  "description": "Vega-Lite chart loader for marimo export bundles.",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "oxlint .",
    "format": "oxfmt --write ."
  },
  "dependencies": {
    "@marimo-team/export-reader": "workspace:*",
    "vega-embed": "^7.1.0"
  }
}
