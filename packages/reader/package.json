{
  "name": "@marimo-team/export-reader",
  "version": "0.0.0",
  "description": "Browser-side reader for finished marimo export bundles.",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "prepack": "pnpm build",
    "test": "rm -rf .test-dist && pnpm build && tsc -p tsconfig.test.build.json && node --test .test-dist/test/*.test.js; status=$?; rm -rf .test-dist; exit $status",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "lint": "oxlint .",
    "format": "oxfmt --write ."
  },
  "dependencies": {
    "fflate": "0.8.2"
  }
}
